# SB 54 portal: publish ONLY the static site at the repo root.
# Keep the private drafting factory, internal planning, build scripts, and the
# AI working guide out of this PUBLIC repo. They stay in your local folder / GDrive.

content_factory/
docs/
tools/
CLAUDE.md
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/

_captest*

# Step 0 gating prototype (not part of the deployed site)
prototypes/
