# SB 54 portal — publish ONLY the static site (public/).
# Keep the private drafting factory, internal planning, and build scripts
# out of this PUBLIC repo. They stay in your local folder / GDrive.

content_factory/
docs/
tools/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/

# Actions workflow not used with branch/root deploy
.github/
_captest*
